# CNC Assistant v2

Чистый Node.js + TypeScript проект для Railway с mobile-first интерфейсом под iPhone
(включая safe areas, `100dvh`, камеру и PWA).

## Что внутри

- OpenAI **Responses API**
- Fast/Smart роутер
- Отдельный Supervisor для технических CNC-запросов и фото
- Приём фото экрана стойки с iPhone
- Локальное сжатие фото до отправки
- PWA: можно добавить на главный экран iPhone
- Один Railway service: frontend + backend вместе
- API-ключ существует только на сервере

## 1. Локальный запуск

Нужен Node.js 20+.

```bash
npm install
cp .env.example .env
```

Открой `.env` и вставь НОВЫЙ ключ:

```env
OPENAI_API_KEY=sk-proj_...
FAST_MODEL=gpt-5-mini
SMART_MODEL=gpt-5.6-sol
SUPERVISOR_MODEL=gpt-5.6-sol
ENABLE_SUPERVISOR=true
PORT=3000
```

Потом:

```bash
npm run dev
```

Открой:

```text
http://localhost:3000
```

Проверка компиляции:

```bash
npm run check
npm run build
npm start
```

## 2. Проверить модели, доступные твоему API-ключу

После запуска открой:

```text
http://localhost:3000/api/models
```

Ты увидишь реальные model IDs, доступные именно этому OpenAI API project.
После этого можешь заменить `FAST_MODEL`, `SMART_MODEL` и `SUPERVISOR_MODEL`.

Важно: модель, используемая внутри ChatGPT, и публичный API model ID — не одно и то же.
Поэтому не надо угадывать название модели: бери ID из `/api/models`.

## 3. Railway

Самый простой вариант:

1. Создай новый GitHub repository и залей туда содержимое этой папки.
2. В Railway: **New Project → Deploy from GitHub repo**.
3. В Variables добавь:
   - `OPENAI_API_KEY`
   - `FAST_MODEL`
   - `SMART_MODEL`
   - `SUPERVISOR_MODEL`
   - `ENABLE_SUPERVISOR=true`
4. Build command:
   ```text
   npm run build
   ```
5. Start command:
   ```text
   npm start
   ```
6. Healthcheck path:
   ```text
   /api/health
   ```

Railway сам передаст `PORT`; вручную там его обычно задавать не нужно.

## 4. iPhone 14 Pro

Открой публичный Railway URL в Safari.

Чтобы поставить как приложение:

**Share → Add to Home Screen**

Интерфейс использует:
- `viewport-fit=cover`
- `env(safe-area-inset-top/bottom)`
- `100dvh`
- кнопку камеры `capture="environment"`

То есть Dynamic Island / верхняя safe area и нижний home indicator учтены.

## 5. Старые ключи

Не отзывай старый рабочий ключ до того, как новый Railway service:
- проходит `/api/health`;
- отвечает на обычный текст;
- принимает фото;
- успешно делает Smart + Supervisor ответ.

После этого старые OpenAI API keys можно revoke, а старый Railway service — выключить.

## 6. Стоимость и задержка

`ENABLE_SUPERVISOR=true` делает второй модельный запрос только для Smart/CNC/фото
задач. Это повышает качество контроля, но увеличивает цену и задержку.

Если нужно дешевле:

```env
ENABLE_SUPERVISOR=false
```

## 7. Безопасность

Никогда не:
- вставляй API key в `public/app.js`;
- коммить `.env`;
- показывай ключ в скриншотах;
- отправляй ключ в чат.

Если ключ когда-либо оказался публично — revoke и создай новый.

## Исправления в сборке FINAL v5

Эта сборка уже содержит исправления, найденные при Railway-деплое:

- Express 5: fallback использует `app.use(...)`, а не `app.get("*")`.
- Путь frontend исправлен на `/app/public` после TypeScript build.
- Markdown ответов рендерится безопасно без `innerHTML`.
- Поддерживаются заголовки, списки, **жирный текст**, `inline code` и fenced code blocks.
- `app.js?v=5` принудительно обходит старый browser cache.
- Service Worker v5 использует network-first для HTML/JS/CSS, чтобы новые Railway deploy не зависали на старом кэше.

После замены файлов в GitHub достаточно сделать commit в `main`; Railway должен пересобраться автоматически.


## v6 — cache/Markdown fix

В этой сборке frontend-файлы переименованы в `app-v6.js` и `styles-v6.css`.
Старый Service Worker автоматически удаляется, а HTML/JS/CSS отдаются с `Cache-Control: no-store`.

После успешного deploy в верхней строке состояния должно быть видно `UI v6`.
Если видно `UI v6`, браузер точно загрузил новую сборку.


## v7 — Память станка

В верхней панели появилась кнопка **Память**.

Память разделена на:
- станок и стойку;
- материалы;
- инструмент;
- подтверждённые M-коды / OEM-функции;
- подтверждённые режимы и настройки;
- прочие подтверждённые заметки.

Память хранится в `localStorage` браузера и автоматически отправляется модели с каждым запросом.
Кнопка **Новый чат** очищает только историю диалога — память остаётся.

Для переноса между ноутбуком и iPhone:
1. Память → Экспорт.
2. На другом устройстве: Память → Импорт.

Важно: браузерная память устройства не является облачной базой данных. Для общей синхронизации между
устройствами позже можно подключить Railway Postgres.


## v8 — Защита приложения паролем

Перед облачной памятью добавлена авторизация, потому что публичный Railway URL без защиты
позволил бы постороннему человеку расходовать OpenAI API-баланс.

В Railway Variables обязательно добавь:

```text
APP_PASSWORD=<длинный приватный пароль>
```

Пароль не попадает в frontend-код и не отправляется OpenAI.

После успешного входа сервер устанавливает HttpOnly cookie с `SameSite=Strict`.
Сам пароль в `localStorage` не хранится.

Защищены:
- `/api/chat`
- `/api/models`

Публичными остаются только:
- статический интерфейс;
- `/api/health`;
- `/api/auth/status`;
- `/api/auth/login`.

Следующий логичный этап после проверки v8 — Railway Postgres для общей памяти
между iPhone и ноутбуком.


## v9 — Railway Postgres и общая память

v9 переносит память станка из «только браузерной» в облачную PostgreSQL-базу.

Что происходит:
- сервер автоматически создаёт таблицу `cnc_project_memory`;
- первая браузерная память автоматически переносится в Postgres, если база пустая;
- после этого Postgres становится основной копией;
- ноутбук и iPhone получают одну и ту же память после входа;
- `localStorage` остаётся резервной копией на случай временной недоступности базы;
- GPT-5.6 Sol и Supervisor на сервере читают именно облачную память, если Postgres доступен.

### Railway

1. На Project Canvas нажми `+ New` → `Database` → `PostgreSQL`.
2. В сервисе **CNC-Assistant-v2 → Variables** добавь:
   ```text
   DATABASE_URL=${{Postgres.DATABASE_URL}}
   ```
   Если твой сервис базы называется иначе, замени `Postgres` на его точное имя.
3. Примени staged changes / redeploy приложения.
4. После запуска в Deploy Logs должна появиться строка:
   ```text
   Postgres memory ready
   ```
5. В верхней строке CNC AI появится `память ... cloud`.

Railway официально поддерживает обращение к переменным другого сервиса через
`${{ServiceName.VARIABLE}}`; `DATABASE_URL` PostgreSQL-сервиса предназначен для подключения
из других сервисов того же Railway project.


## v10 — Структурированная база станка

Новая кнопка **База** открывает четыре раздела, хранящиеся в Railway Postgres:

1. **Инструменты**
   - номер инструмента;
   - державка;
   - пластина;
   - ширина;
   - радиус;
   - назначение;
   - подтверждение на станке.

2. **Материалы**
   - материал / марка;
   - состояние;
   - технологические заметки.

3. **M-коды / OEM**
   - код;
   - функция;
   - источник;
   - обязательный флаг «подтверждено именно на этом станке».

4. **Журнал обработки**
   - операция;
   - материал;
   - инструмент;
   - диаметр;
   - S/Vc;
   - F;
   - результат;
   - заметки.

GPT-5.6 Sol и Supervisor автоматически получают эту структурированную базу при каждом запросе.
Неподтверждённые M-коды запрещено трактовать как факт.

### Миграция

Отдельных ручных SQL-миграций не требуется. При старте v10 `initDatabase()` создаёт новые таблицы
`cnc_tools`, `cnc_materials`, `cnc_m_codes`, `cnc_journal` через `CREATE TABLE IF NOT EXISTS`.
Существующая таблица облачной памяти v9 остаётся без изменений.


## v11 — Visual ShopTurn Editor

v11 переводит результат из «текстового совета» в редактируемые ячейки ShopTurn.

Поддерживается первая производственная группа экранов:
- заголовок программы ShopTurn;
- CYCLE951 «Обработка резаньем»;
- CYCLE99 «Нарезание резьбы резцом»;
- CYCLE92 «Отрез»;
- контурная обточка: старт X/Z и элементы прямых/дуг.

### Как пользоваться

1. Открой кнопку **ShopTurn**.
2. Нажми **+ Чертёж** и выбери фото/скрин/чертёж.
3. В поле команды укажи систему координат, если она известна, например:
   `Z0 на переднем торце, X в диаметрах. Составь наружную обработку, M12 и отрез.`
4. Нажми **Собрать**.
5. GPT-5.6 Sol + Supervisor возвращают структуру. Она сразу раскладывается по ячейкам.
6. Пустые/жёлтые ячейки означают «не определено надёжно» — их нужно заполнить вручную.
7. **Сохранить** записывает проект в Railway Postgres. Его можно открыть на iPhone.

### Источник схем полей

Схема v11 собрана по Siemens SINUMERIK 840D sl/828D «Токарная обработка»,
V4.8 SP3, 08/2018:
- ShopTurn, заголовок и T/D/F/S/V — разделы 9.5–9.8;
- CYCLE951 — 10.2.2;
- CYCLE99 — 10.2.6;
- CYCLE92 — 10.2.8;
- токарный контур — 10.3.3–10.3.4.

v11 — редактор/генератор параметров, а не прямой канал управления станком.
Перед переносом параметров в реальную стойку обязательна проверка оператором и симуляция.

## v12 — Drawing → Geometry → Setups → ShopTurn

v12 исправляет главный риск v11: модель больше не пишет значения непосредственно из картинки в маску ShopTurn.
Появился обязательный промежуточный слой **проверенной геометрии чертежа**.

### Новый конвейер

1. Чертёж/фото анализируется отдельно от технологии.
2. Извлекаются только размеры и признаки, связанные с геометрией детали.
3. Серверный валидатор отбрасывает явные статистические выбросы и числа, не согласующиеся с масштабом детали.
4. Деталь делится на физические установки. У каждой установки собственные Z0/workOffset/ZA/ZI/XRA/ZRA.
5. Только после этого GPT строит кадры ShopTurn.
6. Supervisor ещё раз сверяет план с извлечённой геометрией.

Это предназначено, в частности, для защиты от ошибки вида `ZI = 1000`, когда число из рамки/штампа чертежа ошибочно распознаётся как размер детали.

### Поддерживаемые операции v12

- CYCLE951 — обработка резаньем;
- CYCLE99 — резьба резцом;
- CYCLE92 — отрез;
- контурная обточка;
- **CYCLE82 — сверление**;
- **CYCLE84/CYCLE840 — нарезание внутренней резьбы**.

Для CYCLE82 добавлены ShopTurn-поля поверхности/положения, глубины по острию/хвостовику, Z1,
засверловки ZA/FA, сквозного сверления ZD/FD и выдержки DT.

Для CYCLE84/CYCLE840 добавлены режим компенсирующего патрона, sensor/no_sensor, SR/VR,
поверхность/положение, Z1/X1, таблица резьбы/размер/P, режим 1 проход/ломка стружки/удаление стружки,
D, V2 и DT.

### Обратная совместимость

Сохранённый проект v11 с плоским массивом `operations` при открытии автоматически переносится в `Установку 1`.
Новые проекты v12 сохраняются в той же таблице `cnc_shopturn_projects`; новых Railway Variables и ручных SQL-миграций не требуется.


## v13 — фактическая заготовка / Program header

В Visual ShopTurn экран «Заголовок программы» заменён расширенным меню,
повторяющим логику реального SINUMERIK Operate / ShopTurn:

- Work offset;
- запись WO / ZV;
- Blank: Pipe / Cylinder / Polygon / Centered cuboid / none;
- XA;
- XI с abs/inc;
- ZA;
- ZI с abs/inc;
- ZB с abs/inc;
- Retract: Simple / Extended / All;
- XRA / XRI / ZRA / ZRI;
- Tool change point: MCS/WCS;
- XT / ZT;
- S1;
- SC;
- Machined direction.

Есть кнопка **«Применить фактическую заготовку»**. После её нажатия header получает
`headerConfirmed=true`, и при повторной генерации GPT-5.6 Sol получает операторские
значения как неизменяемые ограничения.

Критическое изменение v13: размеры фактической болванки/трубы больше не должны
автоматически выводиться из размеров готовой детали на чертеже.


## v14 — Operator Workflow

Visual ShopTurn теперь ведёт оператора по обязательной последовательности:

1. **Фактическая заготовка** — Blank/XA/XI/Header, подтверждение оператором.
2. **Чертёж** — фото/описание исходных данных.
3. **Геометрия** — проверка распознанных размеров и уверенности.
4. **Установки / Z0** — отдельная ориентация и ноль для каждой установки.
5. **Материал / инструмент** — материал из базы и соответствие каждого T PostgreSQL-базе инструмента.
6. **Технология** — проверка последовательности кадров и критических T/D/F/S/V/геометрических полей.
7. **Проверка** — локальный валидатор + Supervisor; красный конфликт блокирует статус Ready for Simulation.
8. **Simulation** — оператор вручную подтверждает, что выполнил Simulation на SINUMERIK.

Статусы проекта:
- `Черновик`
- `Требует данных`
- `Конфликт — запуск блокирован`
- `Готов к симуляции`
- `Проверено оператором`

Цвета:
- зелёный — операторски подтверждённые данные;
- белый/серый — расчётные/структурированные;
- жёлтый — требует проверки;
- красный — конфликт.

Подтверждения этапов сохраняются вместе с ShopTurn-проектом в Postgres.
Если оператор изменяет данные более раннего этапа, последующие подтверждения автоматически сбрасываются.


## v15 — Light/Dark Liquid Glass

Добавлены две полноценные темы интерфейса:
- Dark Liquid Glass;
- Light Liquid Glass.

Кнопка темы находится в верхней панели рядом с режимом модели.
Выбор сохраняется локально на устройстве через `localStorage`.
При первом запуске без сохранённого выбора используется системная тема iPhone/macOS.

Liquid Glass применяется к:
- верхней панели;
- чату и composer;
- модальным окнам;
- Memory / Base;
- Visual ShopTurn;
- Operator Workflow;
- карточкам/формам/кнопкам.

Используются `backdrop-filter`, полупрозрачные стеклянные слои,
внутренние световые блики и отдельная палитра для светлой темы.
