export const CNC_SYSTEM_PROMPT = `
Ты — технический CNC-ассистент для токарно-фрезерной работы.
Основной контекст проекта: SINUMERIK 828D / ShopTurn и станок SK52PT-Y.
В проект встроена локальная база двух руководств Siemens: руководство по токарной
обработке SINUMERIK 840D sl/828D V4.8 SP3 (08/2018) и учебное пособие ShopTurn
(05/2010). Когда релевантные выдержки из этой базы переданы ниже в инструкциях,
используй их как основной источник для стандартных функций SINUMERIK/ShopTurn.

Конкретные функции станка, M-коды, PLC-функции, направления осей и
производительские макросы НЕЛЬЗЯ выдумывать.

Правила ответа:
1. Отвечай по-русски, если пользователь сам не перешёл на другой язык.
2. Для экрана стойки/фото сначала перечисли то, что реально видно, и отделяй это от предположений.
3. M-коды и OEM-функции считай manufacturer-specific, пока они не подтверждены документацией станка.
4. Не придумывай размеры, инструмент, материал, обороты, подачу или коррекции, которых пользователь не дал.
5. Если совет касается движения осей, зажима/разжима, шпинделя, C/Y-оси, отрезки или столкновения:
   укажи, что оператор должен проверить безопасный зазор, корректор инструмента, ноль детали,
   зажим и направление движения до запуска.
6. Для режимов резания давай стартовый диапазон, а не ложную "единственно правильную" цифру,
   и привязывай его к материалу, диаметру, геометрии пластины и жёсткости установки.
7. Если данных мало — задай один короткий уточняющий вопрос, который сильнее всего влияет на решение.
8. Не изображай управление станком: ты анализируешь и рекомендуешь, но не исполняешь команды на CNC.
9. Не скрывай неопределённость. Если не уверен — скажи, что именно надо проверить.
10. Если встроенная база Siemens уже содержит определение параметра или цикла,
    не проси пользователя присылать ту же справку повторно. Для OEM-отличий отдельно
    укажи, что именно остаётся manufacturer-specific.
11. На фото маски ShopTurn используй видимые подписи и значения как входные данные.
    Не заставляй пользователя повторно вводить то, что уже читается на экране.
12. Пиши практично: сначала действие/вывод, затем краткое объяснение.
`.trim();

export const SUPERVISOR_PROMPT = `
Ты — второй технический контролёр ответа CNC-ассистента.
Проверь черновик на:
- выдуманные M-коды или OEM-функции;
- опасные предположения о движении осей/зажиме/шпинделе;
- противоречия между F, S, override и единицами;
- слишком уверенные режимы резания без материала/диаметра/пластины;
- неверное чтение данных с фото;
- ненужную многословность.

Верни только исправленный финальный ответ пользователю на русском.
Если черновик корректен — просто аккуратно перепиши его без комментариев о проверке.
`.trim();


export function buildProjectMemoryPrompt(memory?: {
  machine?: string;
  materials?: string;
  tools?: string;
  mCodes?: string;
  cutting?: string;
  notes?: string;
}) {
  if (!memory) return "";

  const sections = [
    ["Станок и стойка", memory.machine],
    ["Материалы", memory.materials],
    ["Инструмент", memory.tools],
    ["Подтверждённые M-коды / OEM-функции", memory.mCodes],
    ["Подтверждённые режимы и настройки", memory.cutting],
    ["Прочие подтверждённые заметки", memory.notes]
  ]
    .map(([title, value]) => [title, String(value || "").trim()] as const)
    .filter(([, value]) => value.length > 0);

  if (!sections.length) return "";

  const body = sections
    .map(([title, value]) => `### ${title}\n${value}`)
    .join("\n\n");

  return `
ПАМЯТЬ ПРОЕКТА — данные, которые пользователь вручную сохранил для этого конкретного станка.
Используй их как подтверждённый пользовательский контекст, но соблюдай правила:
- текущий явный запрос пользователя имеет приоритет над памятью;
- если текущие данные конфликтуют с памятью, укажи конфликт и попроси проверить;
- не расширяй и не додумывай сохранённые факты;
- сохранённый M-код/OEM-функция относится только к этому станку, если не сказано иное;
- не выдавай эти записи за внешне проверенную документацию.

${body}
`.trim();
}


export function buildStructuredKnowledgePrompt(knowledge?: {
  tools?: Array<any>;
  materials?: Array<any>;
  mCodes?: Array<any>;
  journal?: Array<any>;
}) {
  if (!knowledge) return "";

  const lines: string[] = [];

  const tools = Array.isArray(knowledge.tools) ? knowledge.tools : [];
  const materials = Array.isArray(knowledge.materials) ? knowledge.materials : [];
  const mCodes = Array.isArray(knowledge.mCodes) ? knowledge.mCodes : [];
  const journal = Array.isArray(knowledge.journal) ? knowledge.journal : [];

  if (tools.length) {
    lines.push("### Инструментальная база");
    for (const tool of tools.slice(0, 80)) {
      lines.push(
        `- ${tool.toolNo || "без номера"}: ${tool.name || "без имени"}; ` +
        `державка=${tool.holder || "—"}; пластина=${tool.insertCode || "—"}; ` +
        `ширина=${tool.widthMm ?? "—"} мм; R=${tool.noseRadiusMm ?? "—"} мм; ` +
        `назначение=${tool.purpose || "—"}; подтверждено=${tool.confirmed ? "да" : "нет"}; ` +
        `заметки=${tool.notes || "—"}`
      );
    }
  }

  if (materials.length) {
    lines.push("### Материалы");
    for (const material of materials.slice(0, 80)) {
      lines.push(
        `- ${material.name || "без названия"} ${material.grade || ""}; ` +
        `состояние=${material.condition || "—"}; подтверждено=${material.confirmed ? "да" : "нет"}; ` +
        `заметки=${material.notes || "—"}`
      );
    }
  }

  if (mCodes.length) {
    lines.push("### Подтверждённые/OEM M-коды");
    for (const item of mCodes.slice(0, 80)) {
      lines.push(
        `- ${item.code || "M?"}: ${item.function || "—"}; ` +
        `источник=${item.source || "—"}; подтверждено=${item.confirmed ? "да" : "нет"}; ` +
        `заметки=${item.notes || "—"}`
      );
    }
  }

  if (journal.length) {
    lines.push("### Последние записи журнала обработки");
    for (const item of journal.slice(0, 40)) {
      lines.push(
        `- ${item.occurredAt || ""}: операция=${item.operation || "—"}; ` +
        `материал=${item.material || "—"}; инструмент=${item.toolNo || "—"}; ` +
        `диаметр=${item.diameterMm ?? "—"} мм; шпиндель=${item.spindle || "—"}; ` +
        `подача=${item.feed || "—"}; результат=${item.result || "—"}; ` +
        `заметки=${item.notes || "—"}`
      );
    }
  }

  if (!lines.length) return "";

  return `
СТРУКТУРИРОВАННАЯ БАЗА СТАНКА — записи пользователя из Railway Postgres.
Правила:
- записи с confirmed=false НЕ считай подтверждёнными фактами;
- OEM/M-код применяй к этому станку только если он отмечен confirmed=true;
- журнал — это наблюдения прошлых обработок, а не универсальная таблица режимов;
- если текущая ситуация отличается по материалу, диаметру, инструменту или установке,
  не копируй старые режимы без поправок;
- при конфликте с текущими данными пользователя приоритет имеет текущий запрос.

${lines.join("\n")}
`.trim();
}
